# .ontos Viewer App

Standalone static viewer app for `.ontos Protocol` files.

This app is local-first:

- bundled sample files are loaded from the same static site
- user-selected files stay in the browser
- no account or backend is required

Build from the repository root:

```bash
npm run build
```

The built app is written to `apps/viewer/dist/`.
