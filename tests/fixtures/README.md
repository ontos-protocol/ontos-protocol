# Test Fixtures

Repository-level fixture notes live here.

The public conformance fixtures are maintained in `spec/conformance/` so third
party implementations can consume them directly.

Snapshot fixtures live in `tests/snapshots/`.
