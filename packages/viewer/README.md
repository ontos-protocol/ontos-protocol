# @ontos-protocol/viewer

Browser viewer primitives for `.ontos Protocol`.

```js
import { defineOntosViewerElement } from "@ontos-protocol/viewer";

defineOntosViewerElement();
```

```html
<ontos-viewer src="/project.ontos"></ontos-viewer>
```

The package also exports pure model helpers for tests and non-DOM integrations.
