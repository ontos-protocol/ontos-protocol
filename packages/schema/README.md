# @ontos-protocol/schema

Shared schema package for `.ontos Protocol`.

Exports:

- `astSchema`
- `ONTOS_FORMAT_VERSION`
- `ONTOS_AST_SCHEMA_VERSION`
- `ONTOS_AST_SCHEMA_ID`

The canonical schema is also available as:

```text
@ontos-protocol/schema/ontos-ast.schema.json
```

